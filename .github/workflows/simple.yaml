name: Shell Commands-1

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: echo "Hello World"
      - name: multiline script
        run: >
          node -v
          npm -v
          echo "my name is Sardar"
